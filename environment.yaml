# conda env create -f environment.yaml
name: squarer
channels:
  - defaults
  - conda-forge
dependencies:
  - black
  - coverage
  - numpy
  - pre-commit
  - pylint
  - pytest
  - tox
  - pip:
      - covdefaults
